# FindPinocchio.cmake

# find_package(PkgConfig REQUIRED)
# pkg_check_modules(PINOCCHIO REQUIRED pinocchio)

# if(PINOCCHIO_FOUND)
#   set(Pinocchio_INCLUDE_DIRS ${PINOCCHIO_INCLUDE_DIRS})
#   set(Pinocchio_LIBRARIES ${PINOCCHIO_LIBRARIES})
#   set(Pinocchio_DEFINITIONS ${PINOCCHIO_CFLAGS_OTHER})
# else()
#   message(FATAL_ERROR "Could not find Pinocchio using pkg-config")
# endif()
